/** Dependency-free control-plane UI served by the team server. Tokens remain tab-local. */
export const TEAM_STUDIO_HTML: string = `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Dry Run Control Plane</title><style>
:root{color-scheme:dark;--bg:#08090c;--surface:#101217;--surface2:#151820;--surface3:#1b1f29;--line:#272c37;--line2:#343b49;--text:#f5f7fb;--muted:#9098a8;--blue:#0a84ff;--blue2:#62adff;--green:#30d158;--amber:#ff9f0a;--red:#ff453a;--purple:#bf5af2;--shadow:0 22px 70px #0009}
*{box-sizing:border-box}html,body{margin:0;min-height:100%;background:var(--bg);color:var(--text);font:14px -apple-system,BlinkMacSystemFont,"SF Pro Text",Inter,system-ui,sans-serif}button,input,select,textarea{font:inherit;color:inherit}button{cursor:pointer}.hidden{display:none!important}.muted{color:var(--muted)}.mono{font-family:"SFMono-Regular",Consolas,monospace}.truncate{white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.login-wrap{min-height:100vh;display:grid;place-items:center;padding:24px;background:radial-gradient(circle at 30% 0,#173151 0,transparent 35%),radial-gradient(circle at 80% 100%,#261638 0,transparent 32%)}.window{width:min(530px,100%);background:#111319e8;border:1px solid #303643;border-radius:22px;box-shadow:var(--shadow);overflow:hidden;backdrop-filter:blur(24px)}.traffic{height:48px;display:flex;align-items:center;gap:8px;padding:0 18px;border-bottom:1px solid var(--line)}.dot{width:12px;height:12px;border-radius:50%}.dot.r{background:#ff5f57}.dot.y{background:#febc2e}.dot.g{background:#28c840}.login-body{padding:34px}.brand{display:flex;align-items:center;gap:13px}.logo{width:43px;height:43px;border-radius:13px;background:linear-gradient(145deg,#7bc3ff,#086bd8 60%,#064692);box-shadow:inset 0 1px #ffffff70,0 9px 28px #0a84ff45;display:grid;place-items:center;font-size:22px;font-weight:800}.brand h1{font-size:25px;margin:0;letter-spacing:-.5px}.brand p{margin:4px 0 0}.login-copy{margin:28px 0 18px;line-height:1.55}.field{width:100%;border:1px solid var(--line2);background:#0c0e12;border-radius:11px;padding:11px 13px;outline:0}.field:focus{border-color:var(--blue);box-shadow:0 0 0 3px #0a84ff25}.login-actions{display:flex;gap:9px;margin-top:11px}.btn{border:0;border-radius:10px;padding:10px 14px;background:var(--blue);font-weight:650;box-shadow:inset 0 1px #ffffff30}.btn.secondary{background:var(--surface3);border:1px solid var(--line2);box-shadow:none}.btn.danger{background:#3d1718;color:#ffb6b2}.btn.small{padding:7px 10px;font-size:12px}.error{color:#ff8a83;margin-top:12px;min-height:18px}
.shell{min-height:100vh;display:grid;grid-template-columns:230px minmax(0,1fr)}.sidebar{position:sticky;top:0;height:100vh;background:#0d0f13;border-right:1px solid #20242d;padding:18px 13px;display:flex;flex-direction:column}.sidebar .brand{padding:6px 8px 22px}.sidebar .logo{width:34px;height:34px;border-radius:10px;font-size:17px}.sidebar h1{font-size:17px}.nav{display:grid;gap:4px}.nav button{display:flex;align-items:center;gap:11px;text-align:left;border:0;background:transparent;color:#b0b6c2;padding:10px 11px;border-radius:9px}.nav button:hover{background:#171a21;color:var(--text)}.nav button.active{background:#1c2633;color:#fff;box-shadow:inset 0 0 0 1px #2a4059}.nav svg{width:17px;height:17px}.side-foot{margin-top:auto;padding:12px 8px;border-top:1px solid var(--line)}.identity{margin-bottom:12px}.identity strong{display:block;color:#e9ebef}.identity span{font-size:12px}.main{min-width:0}.topbar{height:68px;position:sticky;top:0;z-index:10;background:#090b0ed9;backdrop-filter:blur(22px);border-bottom:1px solid #1e222a;display:flex;align-items:center;padding:0 28px;gap:12px}.crumb{font-weight:680;font-size:16px}.spacer{flex:1}.select,.search{background:#12151b;border:1px solid var(--line);border-radius:9px;padding:8px 10px;outline:0}.search{width:220px}.content{padding:28px;max-width:1500px;margin:auto}.view-head{display:flex;align-items:flex-end;justify-content:space-between;gap:18px;margin-bottom:22px}.view-head h2{font-size:27px;letter-spacing:-.6px;margin:0 0 4px}.filters{display:flex;gap:8px;flex-wrap:wrap}.kpis{display:grid;grid-template-columns:repeat(auto-fit,minmax(150px,1fr));gap:12px}.card{background:linear-gradient(180deg,#14171d,#101218);border:1px solid var(--line);border-radius:14px;box-shadow:0 1px #ffffff08}.kpi{padding:17px}.kpi .value{font-size:27px;font-weight:730;letter-spacing:-.5px;margin-top:9px}.kpi .delta{font-size:11px;margin-top:5px}.good{color:var(--green)}.bad{color:var(--red)}.warn{color:var(--amber)}.grid2{display:grid;grid-template-columns:minmax(0,1.55fr) minmax(280px,.75fr);gap:14px;margin-top:14px}.card-head{height:50px;padding:0 16px;display:flex;align-items:center;border-bottom:1px solid var(--line);font-weight:650}.card-body{padding:16px}.chart{height:240px}.chart svg{width:100%;height:100%;overflow:visible}.chart .gridline{stroke:#282d37;stroke-width:1}.chart .area{fill:url(#area)}.chart .line{fill:none;stroke:var(--blue2);stroke-width:2.5;stroke-linecap:round;stroke-linejoin:round}.legend{display:flex;gap:14px;font-size:12px;color:var(--muted)}.legend i{display:inline-block;width:8px;height:8px;border-radius:50%;background:var(--blue);margin-right:5px}.facet{margin-bottom:14px}.facet-row{display:flex;justify-content:space-between;margin-bottom:6px}.bar{height:5px;background:#252a34;border-radius:99px;overflow:hidden}.bar span{display:block;height:100%;background:linear-gradient(90deg,var(--blue),var(--purple));border-radius:99px}.table-card{margin-top:14px;overflow:hidden}.table-scroll{overflow:auto}.table{width:100%;border-collapse:collapse;min-width:760px}.table th{font-size:11px;text-transform:uppercase;letter-spacing:.06em;color:#777f8e;font-weight:650;text-align:left;padding:11px 14px;background:#0e1015;border-bottom:1px solid var(--line)}.table td{padding:12px 14px;border-bottom:1px solid #20242c;vertical-align:middle}.table tr:last-child td{border-bottom:0}.table tbody tr{cursor:pointer}.table tbody tr:hover{background:#171b23}.status{display:inline-flex;align-items:center;gap:6px;font-size:12px;border-radius:99px;padding:4px 8px;background:#20242d}.status:before{content:"";width:6px;height:6px;border-radius:50%;background:var(--muted)}.status.ok,.status.passed,.status.completed{color:#8df1a3;background:#173120}.status.ok:before,.status.passed:before,.status.completed:before{background:var(--green)}.status.error,.status.failed{color:#ff9b95;background:#381a1b}.status.error:before,.status.failed:before{background:var(--red)}.empty{padding:38px;text-align:center;color:var(--muted)}.tag{display:inline-block;font-size:11px;padding:3px 7px;margin:2px;border-radius:6px;background:#202631;color:#adb9ca}.queue-layout{display:grid;grid-template-columns:250px minmax(280px,.8fr) minmax(340px,1.2fr);gap:12px}.queue-list,.review-list{padding:8px}.queue-item,.review-item{border:0;background:transparent;width:100%;text-align:left;border-radius:9px;padding:10px;margin-bottom:3px}.queue-item:hover,.review-item:hover,.queue-item.active,.review-item.active{background:#1b2029}.queue-item strong,.review-item strong{display:block}.queue-item span,.review-item span{font-size:12px;color:var(--muted)}.review-panel{padding:20px}.score{width:100%;accent-color:var(--blue)}textarea.field{min-height:100px;resize:vertical;margin-top:10px}.review-actions{display:flex;gap:8px;margin-top:12px}.target{padding:12px;background:#0b0d11;border:1px solid var(--line);border-radius:10px;margin:12px 0}.drawer-backdrop{position:fixed;inset:0;background:#0008;z-index:29}.drawer{position:fixed;right:0;top:0;bottom:0;width:min(760px,92vw);z-index:30;background:#101218;border-left:1px solid #363c48;box-shadow:-24px 0 80px #000b;display:flex;flex-direction:column}.drawer-head{height:68px;display:flex;align-items:center;gap:12px;padding:0 20px;border-bottom:1px solid var(--line)}.drawer-title{font-size:17px;font-weight:700}.drawer-body{padding:20px;overflow:auto;flex:1}.tabs{display:flex;gap:3px;border-bottom:1px solid var(--line);margin-bottom:16px}.tabs button{border:0;background:transparent;color:var(--muted);padding:9px 12px;border-bottom:2px solid transparent}.tabs button.active{color:#fff;border-color:var(--blue)}.span{position:relative;padding:10px 12px;margin:5px 0;background:#151922;border:1px solid #272d38;border-radius:9px}.span-top{display:flex;justify-content:space-between}.duration{font-size:12px;color:var(--muted)}pre.raw{white-space:pre-wrap;word-break:break-word;font:12px/1.55 "SFMono-Regular",Consolas,monospace;color:#c7d2e2;background:#090b0f;border:1px solid var(--line);border-radius:10px;padding:14px}.message{border:1px solid var(--line);border-radius:10px;padding:12px;margin-bottom:9px}.message .role{text-transform:uppercase;font-size:10px;letter-spacing:.08em;color:var(--blue2);margin-bottom:6px}.toast{position:fixed;bottom:22px;right:22px;z-index:50;background:#252a34;border:1px solid #404755;border-radius:10px;padding:11px 15px;box-shadow:var(--shadow)}
@media(max-width:1050px){.kpis{grid-template-columns:repeat(3,1fr)}.queue-layout{grid-template-columns:220px 1fr}.review-panel{grid-column:1/-1}.grid2{grid-template-columns:1fr}}@media(max-width:720px){.shell{grid-template-columns:1fr}.sidebar{height:auto;position:static;flex-direction:row;overflow:auto}.sidebar .brand,.side-foot{display:none}.nav{display:flex}.nav button span{display:none}.topbar{padding:0 14px}.search{display:none}.content{padding:18px 14px}.kpis{grid-template-columns:repeat(2,1fr)}.queue-layout{grid-template-columns:1fr}.review-panel{grid-column:auto}}
</style></head><body>
<section id="login" class="login-wrap"><div class="window"><div class="traffic"><i class="dot r"></i><i class="dot y"></i><i class="dot g"></i></div><div class="login-body"><div class="brand"><div class="logo">D</div><div><h1>Dry Run</h1><p class="muted">AI quality control plane</p></div></div><p class="login-copy muted">Inspect traces, compare evaluations, investigate regressions, and run human review without sending your data to a hosted vendor.</p><input id="token" class="field mono" type="password" autocomplete="off" placeholder="drk_…"><div class="login-actions"><button class="btn" onclick="connect()">Open workspace</button><button id="oidc" class="btn secondary hidden" onclick="location.href='/api/v1/auth/oidc/login'">Continue with SSO</button></div><div id="login-error" class="error"></div></div></div></section>
<section id="app" class="shell hidden"><aside class="sidebar"><div class="brand"><div class="logo">D</div><div><h1>Dry Run</h1><p class="muted">Control plane</p></div></div><nav class="nav" id="nav">
<button data-view="overview" class="active">◫ <span>Overview</span></button><button data-view="setup">⌘ <span>Setup & import</span></button><button data-view="analytics">⌁ <span>Analytics</span></button><button data-view="intelligence">✦ <span>Intelligence</span></button><button data-view="monitors">◉ <span>Monitors</span></button><button data-view="traces">⑂ <span>Traces</span></button><button data-view="experiments">◇ <span>Experiments</span></button><button data-view="judges">≋ <span>Judge reliability</span></button><button data-view="rules">⚙ <span>Quality rules</span></button><button data-view="reviews">✓ <span>Human review</span></button><button data-view="playground">▷ <span>Playground</span></button><button data-view="governance">♙ <span>Governance</span></button></nav><div class="side-foot"><div class="identity"><strong id="identity-name"></strong><span id="identity-role"></span></div><button class="btn secondary small" onclick="disconnect()">Disconnect</button></div></aside>
<main class="main"><header class="topbar"><span class="crumb" id="crumb">Overview</span><span class="spacer"></span><input id="global-search" class="search" placeholder="Search traces and runs…"><select id="project" class="select" onchange="reload()"></select><button class="btn secondary small" onclick="toggleTheme()" aria-label="Toggle contrast">◐</button></header><div class="content" id="content"></div></main></section>
<div id="drawer-root"></div><div id="toast" class="toast hidden"></div>
<script>
let auth='',me=null,state={view:'overview',events:[],traces:[],experiments:[],rules:[],onlineResults:[],monitors:[],playgroundRuns:[],queues:[],intelligence:[],judgeReports:[],selectedQueue:null,selectedReview:null};const el=id=>document.getElementById(id);const names={overview:'Overview',setup:'Setup & import',analytics:'Production analytics',intelligence:'Production intelligence',monitors:'Quality monitors',traces:'Traces',experiments:'Experiments',judges:'Judge reliability',rules:'Quality rules',reviews:'Human review',playground:'Prompt playground',governance:'Organization governance'};
async function api(path,options={}){const headers=Object.assign({'Authorization':'Bearer '+auth},options.body?{'Content-Type':'application/json'}:{},options.headers||{});const response=await fetch(path,Object.assign({},options,{headers}));let body={};try{body=await response.json()}catch{}if(!response.ok)throw new Error(body.error||response.statusText);return body}
async function connect(){auth=el('token').value.trim();if(!auth){el('login-error').textContent='Enter an API key or use organization SSO.';return}try{me=await api('/api/v1/me');el('identity-name').textContent=me.principal.memberName||me.principal.keyName;el('identity-role').textContent=me.principal.role+' · '+me.workspace.name;el('project').innerHTML=me.projects.map(p=>'<option value="'+attr(p.id)+'">'+esc(p.name)+'</option>').join('');el('login').classList.add('hidden');el('app').classList.remove('hidden');await reload()}catch(error){auth='';el('login-error').textContent=error.message}}
async function disconnect(){try{await api('/api/v1/auth/logout',{method:'POST'})}catch{}auth='';me=null;el('token').value='';el('app').classList.add('hidden');el('login').classList.remove('hidden')}
function pid(){return encodeURIComponent(el('project').value)}function base(){return '/api/v1/projects/'+pid()}
async function reload(){closeDrawer();el('content').innerHTML='<div class="empty">Loading workspace data…</div>';try{if(state.view==='overview')await overview();if(state.view==='setup')await setup();if(state.view==='analytics')await analytics();if(state.view==='intelligence')await intelligence();if(state.view==='monitors')await monitors();if(state.view==='traces')await traces();if(state.view==='experiments')await experiments();if(state.view==='judges')await judges();if(state.view==='rules')await qualityRules();if(state.view==='reviews')await reviews();if(state.view==='playground')await playground();if(state.view==='governance')await governance()}catch(error){el('content').innerHTML='<div class="card empty bad">'+esc(error.message)+'</div>'}}
document.getElementById('nav').addEventListener('click',event=>{const button=event.target.closest('button[data-view]');if(!button)return;state.view=button.dataset.view;document.querySelectorAll('#nav button').forEach(x=>x.classList.toggle('active',x===button));el('crumb').textContent=names[state.view];reload()});
el('global-search').addEventListener('keydown',event=>{if(event.key==='Enter'){state.view='traces';document.querySelectorAll('#nav button').forEach(x=>x.classList.toggle('active',x.dataset.view==='traces'));el('crumb').textContent=names.traces;reload()}});
async function overview(){const [summary,usage,events,queues]=await Promise.all([safeApi(base()+'/analytics/summary'),api(base()+'/usage'),safeApi(base()+'/analytics/events?limit=8'),api(base()+'/queues')]);const s=summary.summary||{totals:{count:0,passed:0,failed:0,tokens:0,costUsd:0},passRate:0,latency:{p95Ms:0}};state.queues=queues.queues;let pending=0;for(const q of queues.queues){const page=await api(base()+'/queues/'+encodeURIComponent(q.id)+'/items?status=pending&limit=100');pending+=page.items.length}
el('content').innerHTML=head('Quality overview','Live quality, latency, spend, and review workload across this project.')+kpis([['Runs',num(s.totals.count),'Latest unique resources'],['Pass rate',pct(s.passRate),s.totals.failed+' failed'],['P95 latency',ms(s.latency.p95Ms),'End-to-end'],['Tokens',num(s.totals.tokens),'Observed usage'],['Review backlog',num(pending),'Pending human decisions']])+'<div class="grid2"><section class="card"><div class="card-head">Recent activity</div>'+eventTable(events.events?.items||[])+'</section><section class="card"><div class="card-head">Project health</div><div class="card-body">'+healthRow('Storage',bytes(usage.usage.bytes),Math.min(100,usage.usage.bytes/(usage.quota.maxBytes||1)*100))+healthRow('Files',num(usage.usage.files),Math.min(100,usage.usage.files/(usage.quota.maxFiles||1)*100))+healthRow('Review queues',num(queues.queues.length),Math.min(100,queues.queues.length*10))+'</div></section></div>';bindOpenRows()}
async function setup(){const d=await api('/api/v1/setup/diagnostics');const passed=d.checks.filter(x=>x.status==='pass').length;el('content').innerHTML=head('Setup & import','Verify the deployment, create a zero-cost demo, or migrate existing evaluation data without a hosted account.')+kpis([['Deployment',d.ready?'Ready':'Action needed',d.mode],['Checks',passed+'/'+d.checks.length,'passing now'],['Hosted services','0 required','self-hosted OSS'],['Provider cost','$0','demo and import']])+'<div class="grid2"><section class="card"><div class="card-head">Setup diagnostics</div><div class="card-body">'+d.checks.map(x=>'<div class="span"><div class="span-top"><strong>'+esc(x.title)+'</strong>'+status(x.status)+'</div><div class="duration">'+esc(x.detail)+'</div></div>').join('')+'<button id="rerun-diagnostics" class="btn secondary small">Run diagnostics again</button></div></section><section class="card"><div class="card-head">Five-minute demo</div><div class="card-body"><p class="muted">Create 24 deterministic traces across v1/v2, a production rule, and a review queue. No model or network call is made.</p><button id="seed-demo" class="btn">Create demo workspace</button><p class="muted">Safe to run repeatedly; trace IDs are idempotent.</p></div></section></div><section class="card" style="margin-top:14px"><div class="card-head">Import an existing evaluation export</div><div class="card-body"><div style="display:grid;grid-template-columns:180px 1fr;gap:8px"><select id="import-source" class="select"><option value="langfuse">Langfuse JSON</option><option value="braintrust">Braintrust JSON</option><option value="deepeval">DeepEval JSON</option></select><input id="import-name" class="field" value="migration-import" placeholder="Import name"><input id="import-file" class="field" type="file" accept="application/json,.json" style="grid-column:1/-1"><textarea id="import-json" class="field mono" style="grid-column:1/-1;min-height:180px" placeholder="Paste the exported JSON here. Secrets are redacted during conversion."></textarea></div><div class="review-actions"><button id="preview-import" class="btn secondary">Preview import</button><button id="run-import" class="btn">Import into project</button></div><div id="import-result" class="target hidden"></div></div></section>';el('rerun-diagnostics').onclick=setup;el('seed-demo').onclick=async()=>{const out=await api(base()+'/demo',{method:'POST',body:JSON.stringify({count:24})});toast(out.traces+' demo traces created');await setup()};el('import-file').onchange=async event=>{const file=event.target.files?.[0];if(file)el('import-json').value=await file.text()};const runImport=async dryRun=>{let input;try{input=JSON.parse(el('import-json').value)}catch{toast('Choose or paste a valid JSON export');return}const out=await api(base()+'/import',{method:'POST',body:JSON.stringify({source:el('import-source').value,name:el('import-name').value,input,dryRun})});const result=el('import-result');result.classList.remove('hidden');result.innerHTML='<strong>'+(dryRun?'Preview complete':'Import complete')+'</strong><pre class="raw">'+esc(JSON.stringify(out,null,2))+'</pre>';if(!dryRun)toast('Evaluation data imported')};el('preview-import').onclick=()=>runImport(true);el('run-import').onclick=()=>runImport(false)}
async function analytics(){const query=filterQuery();const [summary,series,facets,events]=await Promise.all([api(base()+'/analytics/summary'+query),api(base()+'/analytics/timeseries'+mergeQuery(query,'interval=day')),api(base()+'/analytics/facets'+query),api(base()+'/analytics/events'+mergeQuery(query,'limit=100'))]);const s=summary.summary;state.events=events.events.items;el('content').innerHTML=head('Production analytics','Slice quality and operational signals by environment, release, provider, model, or tag.',analyticsFilters())+kpis([['Events',num(s.totals.count),'Filtered resources'],['Pass rate',pct(s.passRate),s.totals.failed+' failed'],['P50',ms(s.latency.p50Ms),'Median latency'],['P95',ms(s.latency.p95Ms),'Tail latency'],['Cost',usd(s.totals.costUsd),'Recorded spend']])+'<div class="grid2"><section class="card"><div class="card-head"><span>Quality over time</span><span class="spacer"></span><span class="legend"><span><i></i>Pass rate</span></span></div><div class="card-body chart">'+chart(series.series.points)+'</div></section><section class="card"><div class="card-head">Dimensions</div><div class="card-body">'+facetBlocks(facets.facets)+'</div></section></div><section class="card table-card"><div class="card-head">Filtered resources</div>'+eventTable(state.events)+'</section>';bindOpenRows();bindFilters()}
async function intelligence(){const out=await api(base()+'/intelligence?limit=50');state.intelligence=out.reports||[];const r=state.intelligence[0];const body=r?intelligenceReport(r):'<div class="card empty">No comparison yet. Select two release labels to create an evidence-backed report.</div>';el('content').innerHTML=head('Production intelligence','Detect statistically material regressions, distribution drift, robust anomalies, failure clusters, and likely root causes.')+(r?kpis([['Verdict',r.verdict,num(r.sample.candidate)+' candidate events'],['Pass-rate delta',signedPct(r.releaseComparison.passRate.absoluteDelta),r.releaseComparison.passRate.statisticallyDistinct?'95% intervals do not overlap':'not statistically distinct'],['Drift signals',num(r.drift.filter(x=>x.drifted).length),'JS divergence + KS'],['Anomalies',num(r.anomalies.length),'median absolute deviation'],['Failure clusters',num(r.failureClusters.length),num(r.sample.failures)+' failed events']]):'')+'<div class="grid2"><section>'+body+'</section><section class="card"><div class="card-head">Compare releases</div><div class="card-body"><label class="muted">Baseline release</label><input id="intel-base" class="field mono" value="v1" placeholder="v1"><div style="height:10px"></div><label class="muted">Candidate release</label><input id="intel-candidate" class="field mono" value="v2" placeholder="v2"><div style="height:10px"></div><label class="muted">Minimum events per window</label><input id="intel-min" class="field" type="number" min="1" value="20"><button id="run-intelligence" class="btn" style="margin-top:11px">Analyze production evidence</button><p class="muted">No hosted service or paid model is used. The report is derived from your own analytics events.</p></div></section></div>';el('run-intelligence').onclick=async()=>{const button=el('run-intelligence');button.disabled=true;try{const result=await api(base()+'/intelligence',{method:'POST',body:JSON.stringify({baseline:{release:el('intel-base').value},candidate:{release:el('intel-candidate').value},minimumEvents:Number(el('intel-min').value)})});toast('Verdict: '+result.report.verdict);intelligence()}finally{button.disabled=false}}}
function intelligenceReport(r){const causes=(r.rootCauses||[]).slice(0,8),drift=(r.drift||[]).filter(x=>x.drifted).slice(0,8),clusters=(r.failureClusters||[]).slice(0,6);return '<section class="card"><div class="card-head"><span>Latest evidence report</span><span class="spacer"></span>'+status(r.verdict)+'</div><div class="card-body">'+(r.reasons||[]).map(x=>'<div class="span">'+esc(x)+'</div>').join('')+'<h3>Likely root causes</h3>'+(causes.map(x=>'<div class="facet"><div class="facet-row"><span>'+esc(x.dimension)+' · <strong>'+esc(x.value)+'</strong></span><span>'+Number(x.lift).toFixed(2)+'× lift</span></div><div class="bar"><span style="width:'+Math.min(100,Math.max(2,x.failureRate*100))+'%"></span></div><div class="duration">'+num(x.failures)+'/'+num(x.events)+' failures · φ '+Number(x.phi).toFixed(3)+'</div></div>').join('')||'<div class="muted">No elevated failure correlate.</div>')+'<h3>Material drift</h3>'+(drift.map(x=>'<span class="tag">'+esc(x.dimension)+' '+Number(x.score).toFixed(3)+'</span>').join('')||'<span class="muted">No material drift.</span>')+'<h3>Failure clusters</h3>'+(clusters.map(x=>'<div class="span"><div class="span-top"><strong>'+esc(x.name)+'</strong><span>'+num(x.count)+'</span></div><div class="duration">'+esc(x.status)+' · '+pct(x.shareOfFailures)+' of failures</div></div>').join('')||'<div class="muted">No failures in candidate window.</div>')+'</div></section>'}
async function judges(){const out=await api(base()+'/judge-reliability?limit=50');state.judgeReports=out.reports||[];const r=state.judgeReports[0];el('content').innerHTML=head('Judge reliability','Calibrate local or remote judges against gold labels; measure repeatability, pair agreement, bias, ensemble uncertainty, and drift.')+(r?kpis([['Gate',r.gate.passed?'passed':'failed',num(r.gate.violations.length)+' violations'],['Judges',num(r.judges),num(r.samples)+' observations'],['Targets',num(r.targets),num(r.ensemble.filter(x=>x.uncertain).length)+' uncertain'],['Mean repeatability',pct(avg(r.repeatability.map(x=>x.repeatability))),'repeated decisions'],['Pair agreement',pct(avg(r.pairAgreement.map(x=>x.binaryAgreement))),'thresholded decisions']]):'')+'<div class="grid2"><section class="card"><div class="card-head">Reliability evidence</div><div class="card-body">'+(r?judgeReport(r):'<div class="empty">No judge report yet.</div>')+'</div></section><section class="card"><div class="card-head">Analyze observations</div><div class="card-body"><p class="muted">Each row needs targetId, judgeId, run, score, and optionally expected, latencyMs, tokens, occurredAt.</p><textarea id="judge-observations" class="field mono">[{"targetId":"gold-1","judgeId":"local-a","run":1,"score":0.92,"expected":true},{"targetId":"gold-1","judgeId":"local-a","run":2,"score":0.88,"expected":true},{"targetId":"gold-1","judgeId":"local-b","run":1,"score":0.81,"expected":true},{"targetId":"gold-2","judgeId":"local-a","run":1,"score":0.12,"expected":false},{"targetId":"gold-2","judgeId":"local-b","run":1,"score":0.2,"expected":false}]</textarea><button id="run-judges" class="btn" style="margin-top:10px">Run reliability audit</button><p class="muted">Bootstrap is deterministically seeded; identical input yields identical uncertainty intervals.</p></div></section></div>';el('run-judges').onclick=async()=>{const button=el('run-judges');button.disabled=true;try{const observations=JSON.parse(el('judge-observations').value);const result=await api(base()+'/judge-reliability',{method:'POST',body:JSON.stringify({observations,policy:{minimumGoldSamples:1}})});toast(result.report.gate.passed?'Judge gate passed':'Judge gate failed');judges()}finally{button.disabled=false}}}
function judgeReport(r){return ((r.gate.violations||[]).map(x=>'<div class="span bad">'+esc(x)+'</div>').join('')||'<div class="span good">All configured reliability gates passed.</div>')+'<h3>Judge profiles</h3>'+(r.profiles||[]).map(p=>'<div class="span"><div class="span-top"><strong>'+esc(p.judgeId)+'</strong><span>'+pct(p.meanScore)+'</span></div><div class="duration">σ '+Number(p.standardDeviation).toFixed(3)+' · '+ms(p.averageLatencyMs)+' · '+num(p.totalTokens)+' tokens'+(p.calibration?' · Brier '+Number(p.calibration.brierScore).toFixed(3)+' · ECE '+Number(p.calibration.expectedCalibrationError).toFixed(3):'')+'</div></div>').join('')+'<h3>Repeatability</h3>'+(r.repeatability||[]).map(x=>healthRow(x.judgeId,pct(x.repeatability),x.repeatability*100)).join('')+'<h3>Pair agreement</h3>'+((r.pairAgreement||[]).map(x=>'<div class="span"><strong>'+esc(x.leftJudgeId)+' / '+esc(x.rightJudgeId)+'</strong><div class="duration">'+pct(x.binaryAgreement)+' agreement · κ '+(x.cohensKappa==null?'—':Number(x.cohensKappa).toFixed(3))+' · r '+(x.pearson==null?'—':Number(x.pearson).toFixed(3))+'</div></div>').join('')||'<div class="muted">Add a second judge to measure agreement.</div>')}
async function monitors(){const response=await api(base()+'/monitors');state.monitors=response.monitors;const histories=await Promise.all(state.monitors.map(m=>safeApi(base()+'/monitors/'+encodeURIComponent(m.id)+'/results?limit=1')));const latest=Object.fromEntries(state.monitors.map((m,i)=>[m.id,histories[i].results?.[0]]));const healthy=Object.values(latest).filter(x=>x?.status==='healthy').length,breached=Object.values(latest).filter(x=>x?.status==='breached').length,unknown=state.monitors.length-healthy-breached;el('content').innerHTML=head('Quality monitors','Persist production SLOs for pass rate, failure rate, tail latency, tokens, and cost; retain every evaluation as evidence.')+kpis([['Monitors',num(state.monitors.length),'revisioned policies'],['Healthy',num(healthy),'latest window'],['Breached',num(breached),'requires action'],['Awaiting data',num(unknown),'not yet evaluated or sparse'],['License cost','$0','self-hosted monitoring']])+'<div class="grid2"><section class="card"><div class="card-head">Production SLOs</div><div class="card-body">'+(state.monitors.map(m=>{const r=latest[m.id];return '<div class="span"><div class="span-top"><strong>'+esc(m.name)+'</strong>'+status(r?.status||'not-evaluated')+'</div><div class="duration">r'+num(m.revision)+' · '+num(m.windowMinutes)+' minute window · minimum '+num(m.minEvents)+' events</div><div class="muted" style="margin-top:7px">'+esc(Object.entries(m.thresholds).map(x=>x[0]+' '+x[1]).join(' · '))+'</div><div style="margin-top:9px"><button class="btn secondary small" data-evaluate-monitor="'+attr(m.id)+'">Evaluate now</button></div></div>'}).join('')||'<div class="empty">No quality monitor yet.</div>')+'</div></section><section class="card"><div class="card-head">Create monitor</div><div class="card-body"><input id="monitor-name" class="field" value="Production quality SLO" placeholder="Monitor name"><div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:8px"><input id="monitor-pass" class="field" type="number" min="0" max="1" step="0.01" value="0.95" placeholder="Minimum pass rate"><input id="monitor-p95" class="field" type="number" min="1" value="5000" placeholder="Maximum p95 ms"><input id="monitor-window" class="field" type="number" min="1" value="60" placeholder="Window minutes"><input id="monitor-events" class="field" type="number" min="1" value="20" placeholder="Minimum events"></div><button id="create-monitor" class="btn" style="margin-top:10px">Create monitor</button></div></section></div>';bindMonitors()}
function bindMonitors(){document.querySelectorAll('[data-evaluate-monitor]').forEach(button=>button.onclick=async()=>{button.disabled=true;try{const out=await api(base()+'/monitors/'+encodeURIComponent(button.dataset.evaluateMonitor)+'/evaluate',{method:'POST',body:'{}'});toast('Monitor '+out.result.status);await monitors()}finally{button.disabled=false}});el('create-monitor').onclick=async()=>{const body={name:el('monitor-name').value,windowMinutes:Number(el('monitor-window').value),minEvents:Number(el('monitor-events').value),thresholds:{minPassRate:Number(el('monitor-pass').value),maxP95LatencyMs:Number(el('monitor-p95').value)}};await api(base()+'/monitors',{method:'POST',body:JSON.stringify(body)});toast('Quality monitor created');monitors()}}
function analyticsFilters(){return '<div class="filters"><select id="f-kind" class="select"><option value="">All types</option><option value="trace">Traces</option><option value="experiment">Experiments</option></select><select id="f-status" class="select"><option value="">All statuses</option><option value="ok">Passed traces</option><option value="error">Failed traces</option><option value="passed">Passed experiments</option><option value="failed">Failed experiments</option></select><select id="f-range" class="select"><option value="7">Last 7 days</option><option value="30" selected>Last 30 days</option><option value="90">Last 90 days</option><option value="0">All time</option></select><button id="apply-filters" class="btn small">Apply</button></div>'}
function filterQuery(){return ''}function bindFilters(){el('apply-filters').onclick=()=>{const p=new URLSearchParams();const kind=el('f-kind').value,status=el('f-status').value,days=Number(el('f-range').value);if(kind)p.set('kind',kind);if(status)p.set('status',status);if(days)p.set('since',new Date(Date.now()-days*86400000).toISOString());const q=p.toString();window.__analyticsQuery=q?'?'+q:'';analyticsWith(window.__analyticsQuery)}}async function analyticsWith(query){const old=filterQuery;window.__dryFilter=query;await analytics()}function filterQuery(){return window.__dryFilter||''}
async function traces(){const q=el('global-search').value.trim();const response=await api(base()+'/traces?limit=200'+(q?'&q='+encodeURIComponent(q):''));state.traces=response.traces;el('content').innerHTML=head('Traces','Inspect nested agent, LLM, retrieval, tool, and scorer spans.')+'<section class="card table-card"><div class="card-head">'+num(state.traces.length)+' traces</div>'+traceTable(state.traces)+'</section>';bindOpenRows()}
async function experiments(){const response=await api(base()+'/experiments?limit=200');state.experiments=response.experiments;el('content').innerHTML=head('Experiments','Compare datasets, score distributions, confidence intervals, regressions, and provenance.')+'<section class="card table-card"><div class="card-head">'+num(state.experiments.length)+' experiment runs</div>'+experimentTable(state.experiments)+'</section>';bindOpenRows()}
async function qualityRules(){const [rules,results]=await Promise.all([api(base()+'/online/rules'),api(base()+'/online/results?limit=200')]);state.rules=rules.rules;state.onlineResults=results.results;const failed=state.onlineResults.filter(x=>!x.passed).length;el('content').innerHTML=head('Quality rules','Sample production traffic, run deterministic or local semantic checks, and route failures to human review.','<button id="run-rules" class="btn">Evaluate stored traces</button>')+kpis([['Active rules',num(state.rules.filter(x=>x.enabled).length),'of '+state.rules.length],['Evaluations',num(state.onlineResults.length),'idempotent results'],['Failures',num(failed),'sent to review'],['Pass rate',pct(state.onlineResults.length?(state.onlineResults.length-failed)/state.onlineResults.length:0),'across evaluated traces'],['Provider cost','$0','deterministic or local judge']])+'<div class="grid2"><section class="card"><div class="card-head">Rules</div><div class="card-body">'+(state.rules.map(rule=>'<div class="span"><div class="span-top"><strong>'+esc(rule.name)+'</strong>'+status(rule.enabled?'active':'disabled')+'</div><div class="duration">r'+num(rule.revision)+' · '+Math.round((rule.filter.sampleRate??1)*100)+'% sample · '+num(rule.checks.length)+' checks</div><div style="margin-top:9px"><button class="btn secondary small" data-toggle-rule="'+attr(rule.id)+'" data-enabled="'+attr(rule.enabled)+'">'+(rule.enabled?'Disable':'Enable')+'</button> <button class="btn danger small" data-delete-rule="'+attr(rule.id)+'">Delete</button></div></div>').join('')||'<div class="empty">No rules yet.</div>')+'</div></section><section class="card"><div class="card-head">Create a production gate</div><div class="card-body"><input id="rule-name" class="field" placeholder="Rule name" value="Production safety"><div style="height:8px"></div><input id="rule-tags" class="field" placeholder="Required tags, comma separated" value="production"><div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:8px"><input id="rule-latency" class="field" type="number" min="1" value="5000" placeholder="Max latency ms"><input id="rule-sample" class="field" type="number" min="0" max="1" step="0.05" value="1" placeholder="Sample rate"></div><label class="muted" style="display:block;margin:12px 0"><input id="rule-tools" type="checkbox" checked> Fail on tool errors and repeated tool loops</label><button id="create-rule" class="btn">Create rule</button></div></section></div><section class="card table-card"><div class="card-head">Recent online evaluations</div>'+onlineResultTable(state.onlineResults)+'</section>';bindRuleActions()}
function onlineResultTable(items){if(!items.length)return '<div class="empty">No trace has matched a quality rule yet.</div>';return '<div class="table-scroll"><table class="table"><thead><tr><th>Rule / trace</th><th>Result</th><th>Checks</th><th>Duration</th><th>Evaluated</th></tr></thead><tbody>'+items.map(x=>'<tr data-open="local" data-kind="trace" data-id="'+attr(x.traceId)+'"><td><strong>'+esc(x.ruleName)+'</strong><div class="mono muted">'+esc(x.traceName)+' · '+esc(x.traceId)+'</div></td><td>'+status(x.passed?'passed':'failed')+'</td><td>'+num(x.checks.length)+'</td><td>'+ms(x.durationMs)+'</td><td>'+date(x.evaluatedAt)+'</td></tr>').join('')+'</tbody></table></div>'}
function bindRuleActions(){el('run-rules').onclick=async()=>{el('run-rules').disabled=true;try{const out=await api(base()+'/online/batch',{method:'POST',body:'{}'});toast('Evaluated '+out.summary.evaluated+' rule matches');await qualityRules()}finally{el('run-rules').disabled=false}};el('create-rule').onclick=async()=>{const checks=[{type:'maxDuration',ms:Number(el('rule-latency').value)}];if(el('rule-tools').checked)checks.push({type:'noToolErrors'},{type:'noRepeatedToolCalls'});const body={name:el('rule-name').value,filter:{sampleRate:Number(el('rule-sample').value),tags:el('rule-tags').value.split(',').map(x=>x.trim()).filter(Boolean)},checks,action:{queueName:'Online evaluation failures',labels:['production-failure']}};await api(base()+'/online/rules',{method:'POST',body:JSON.stringify(body)});toast('Quality rule created');qualityRules()};document.querySelectorAll('[data-toggle-rule]').forEach(button=>button.onclick=async()=>{await api(base()+'/online/rules/'+encodeURIComponent(button.dataset.toggleRule),{method:'PATCH',body:JSON.stringify({enabled:button.dataset.enabled!=='true'})});qualityRules()});document.querySelectorAll('[data-delete-rule]').forEach(button=>button.onclick=async()=>{if(!confirm('Delete this quality rule? Historical results remain.'))return;await api(base()+'/online/rules/'+encodeURIComponent(button.dataset.deleteRule),{method:'DELETE'});qualityRules()});bindOpenRows()}
async function playground(){const response=await api(base()+'/playground/runs');state.playgroundRuns=response.runs;el('content').innerHTML=head('Prompt playground','Compare prompt and local-model variants side by side, then promote the winner into an immutable experiment and production prompt label.')+'<div class="grid2"><section class="card"><div class="card-head">New local comparison</div><div class="card-body"><input id="pg-name" class="field" value="Support prompt comparison" placeholder="Run name"><div style="height:8px"></div><input id="pg-prompt-name" class="field" value="support-answer" placeholder="Prompt registry name"><div style="display:grid;grid-template-columns:1.3fr 1fr;gap:8px;margin-top:8px"><input id="pg-endpoint" class="field mono" value="http://127.0.0.1:11434/v1" placeholder="Local OpenAI-compatible endpoint"><input id="pg-model" class="field mono" value="qwen3:8b" placeholder="Local model"></div><p class="muted">Variant A</p><textarea id="pg-a" class="field">Answer accurately and concisely: {{input}}</textarea><p class="muted">Variant B</p><textarea id="pg-b" class="field">You are a careful support specialist. Give a direct, grounded answer to: {{input}}</textarea><p class="muted">Dataset cases (JSON array)</p><textarea id="pg-cases" class="field">[{"id":"refund","input":"What is the refund window?","expected":"30 days"}]</textarea><div style="display:flex;gap:8px;margin-top:10px"><select id="pg-scorer" class="select"><option value="exact">Exact match</option><option value="contains" selected>Contains expected</option><option value="semantic">Local semantic judge</option></select><button id="pg-run" class="btn">Run comparison</button></div></div></section><section class="card"><div class="card-head">Saved comparisons</div><div class="card-body">'+(state.playgroundRuns.map(run=>'<div class="span"><div class="span-top"><strong>'+esc(run.name)+'</strong>'+status(run.status)+'</div><div class="duration">'+date(run.createdAt)+' · winner '+esc(run.winner||'none')+'</div>'+(run.summaries||[]).map(s=>'<div style="display:flex;align-items:center;gap:8px;margin-top:8px"><span class="'+(s.variantId===run.winner?'good':'muted')+'">'+(s.variantId===run.winner?'★':'·')+' '+esc(s.name)+' · '+pct(s.meanScore)+' · '+pct(s.passRate)+' pass</span><span class="spacer"></span><button class="btn secondary small" data-promote-run="'+attr(run.id)+'" data-variant="'+attr(s.variantId)+'">Promote</button></div>').join('')+'</div>').join('')||'<div class="empty">No playground runs yet.</div>')+'</div></section></div>';bindPlayground()}
function bindPlayground(){el('pg-run').onclick=async()=>{const button=el('pg-run');button.disabled=true;button.textContent='Running local matrix…';try{const model=el('pg-model').value;const body={name:el('pg-name').value,promptName:el('pg-prompt-name').value,endpoint:el('pg-endpoint').value,variants:[{id:'candidate-a',name:'Candidate A',template:el('pg-a').value,model:model,temperature:0},{id:'candidate-b',name:'Candidate B',template:el('pg-b').value,model:model,temperature:0}],cases:JSON.parse(el('pg-cases').value),scorer:{type:el('pg-scorer').value},concurrency:4};const out=await api(base()+'/playground/runs',{method:'POST',body:JSON.stringify(body)});toast('Winner: '+out.run.winner);await playground()}finally{button.disabled=false;button.textContent='Run comparison'}};document.querySelectorAll('[data-promote-run]').forEach(button=>button.onclick=async()=>{const out=await api(base()+'/playground/runs/'+encodeURIComponent(button.dataset.promoteRun)+'/promote',{method:'POST',body:JSON.stringify({variantId:button.dataset.variant,label:'production'})});toast('Promoted prompt v'+out.promoted.prompt.version);playground()})}
async function governance(){const [org,roles,groups,members,keys]=await Promise.all([api('/api/v1/admin/organization'),safeApi('/api/v1/admin/roles'),safeApi('/api/v1/admin/groups'),safeApi('/api/v1/admin/members'),safeApi('/api/v1/admin/keys')]);const canAdmin=Array.isArray(me.principal.capabilities)&&me.principal.capabilities.some(x=>['manage-organization','manage-groups','manage-roles'].includes(x));el('content').innerHTML=head('Organization governance','Manage custom least-privilege roles, live groups, project scopes, service identities, key rotation, and auditable administration.')+kpis([['Organization',org.organization.name,org.organization.slug],['Groups',num((groups.groups||[]).length),'dynamic project scopes'],['Custom roles',num((roles.roles||[]).length),'capability ceilings'],['Members',num((members.members||[]).length),'managed identities'],['API identities',num((keys.keys||[]).length),'keys and service accounts']])+'<div class="grid2"><section class="card"><div class="card-head">Groups and roles</div><div class="card-body"><h3>Groups</h3>'+((groups.groups||[]).map(g=>'<div class="span"><div class="span-top"><strong>'+esc(g.name)+'</strong><span>r'+num(g.revision)+'</span></div><div class="duration">'+num(g.memberIds.length)+' members · '+(g.projectIds?num(g.projectIds.length)+' projects':'all projects')+'</div></div>').join('')||'<div class="muted">No custom groups.</div>')+'<h3>Custom roles</h3>'+((roles.roles||[]).map(r=>'<div class="span"><div class="span-top"><strong>'+esc(r.name)+'</strong><span>'+esc(r.baseRole)+' ceiling</span></div><div class="duration">'+esc(r.capabilities.join(' · '))+'</div></div>').join('')||'<div class="muted">No custom roles.</div>')+'</div></section><section class="card"><div class="card-head">Administration</div><div class="card-body">'+(canAdmin?'<label class="muted">Organization name</label><input id="org-name" class="field" value="'+attr(org.organization.name)+'"><button id="save-org" class="btn" style="margin-top:8px">Save organization</button><hr style="border:0;border-top:1px solid var(--line);margin:18px 0"><label class="muted">New group</label><input id="group-name" class="field" placeholder="Evaluation team"><button id="create-group" class="btn secondary" style="margin-top:8px">Create project group</button><hr style="border:0;border-top:1px solid var(--line);margin:18px 0"><label class="muted">New custom role</label><input id="role-name" class="field" placeholder="Quality operator"><button id="create-role" class="btn secondary" style="margin-top:8px">Create operator role</button>':'<div class="empty">Your role can inspect the organization but cannot change governance.</div>')+'</div></section></div>';if(el('save-org'))el('save-org').onclick=async()=>{await api('/api/v1/admin/organization',{method:'PATCH',body:JSON.stringify({name:el('org-name').value})});toast('Organization updated');governance()};if(el('create-group'))el('create-group').onclick=async()=>{await api('/api/v1/admin/groups',{method:'POST',body:JSON.stringify({name:el('group-name').value,memberIds:[],projectIds:[el('project').value]})});toast('Group created');governance()};if(el('create-role'))el('create-role').onclick=async()=>{await api('/api/v1/admin/roles',{method:'POST',body:JSON.stringify({name:el('role-name').value,baseRole:'editor',capabilities:['read','ingest','annotate','manage-prompts']})});toast('Custom role created');governance()}}
async function reviews(){const response=await api(base()+'/queues?limit=200');state.queues=response.queues;if(!state.selectedQueue&&state.queues[0])state.selectedQueue=state.queues[0].id;let items=[],agreement={},calibration=[],aging={};if(state.selectedQueue){const queue=encodeURIComponent(state.selectedQueue);const values=await Promise.all([api(base()+'/queues/'+queue+'/items?limit=200'),safeApi(base()+'/queues/'+queue+'/agreement'),safeApi(base()+'/queues/'+queue+'/calibration'),safeApi(base()+'/queues/'+queue+'/aging')]);items=values[0].items;agreement=values[1].agreement||{};calibration=values[2].calibration||[];aging=values[3].aging||{}}el('content').innerHTML=head('Human review','Run single, double-blind, or adjudicated programs with deterministic assignment, SLA aging, gold calibration, consensus, and optimistic concurrency.')+kpis([['Completed ratings',num(agreement.ratings),'labeled decisions'],['Reviewers',num(agreement.reviewers),'independent raters'],['Overlap',num(agreement.overlappingTargets),'multiply-rated targets'],['Observed agreement',agreement.observedAgreement==null?'—':pct(agreement.observedAgreement),num(agreement.pairComparisons)+' rating pairs'],['Krippendorff α',agreement.krippendorffAlpha==null?'—':Number(agreement.krippendorffAlpha).toFixed(3),'nominal labels'],['SLA overdue',num(aging.overdue),'of '+num((aging.pending||0)+(aging.claimed||0))+' open assignments']])+reviewOperations(calibration,aging)+'<div class="queue-layout" style="margin-top:14px"><section class="card"><div class="card-head">Review programs</div><div class="queue-list">'+(state.queues.map(q=>'<button class="queue-item '+(q.id===state.selectedQueue?'active':'')+'" data-queue="'+attr(q.id)+'"><strong>'+esc(q.name)+'</strong><span>'+esc(q.mode||'single')+' · '+esc(q.assignment||'manual')+' · '+num(q.reviewersPerTarget||1)+' reviewer(s)</span></button>').join('')||'<div class="empty">No queues</div>')+'</div></section><section class="card"><div class="card-head">Items</div><div class="review-list">'+(items.map(item=>'<button class="review-item '+(state.selectedReview?.id===item.id?'active':'')+'" data-review="'+attr(item.id)+'"><strong>'+esc(item.target.type)+' · '+esc(item.target.id)+'</strong><span>'+status(item.status)+' Priority '+num(item.priority)+'</span></button>').join('')||'<div class="empty">Queue is empty</div>')+'</div></section><section class="card review-panel" id="review-panel">'+reviewPanel(state.selectedReview)+'</section></div>';document.querySelectorAll('[data-queue]').forEach(b=>b.onclick=()=>{state.selectedQueue=b.dataset.queue;state.selectedReview=null;reviews()});document.querySelectorAll('[data-review]').forEach(b=>b.onclick=()=>{state.selectedReview=items.find(x=>x.id===b.dataset.review);reviews()});bindReview();bindReviewProgram()}
function reviewOperations(calibration,aging){const adjudication=state.queues.map(q=>'<option value="'+attr(q.id)+'">'+esc(q.name)+'</option>').join('');return '<div class="grid2"><section class="card"><div class="card-head">Reviewer calibration</div><div class="card-body">'+(calibration.map(x=>healthRow(x.reviewerId,pct(x.accuracy),x.accuracy*100)).join('')||'<span class="muted">Add goldLabel metadata to assignments to measure reviewer accuracy.</span>')+'</div></section><section class="card"><div class="card-head">New review program</div><div class="card-body"><input id="review-queue-name" class="field" placeholder="Safety double-blind review"><div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:8px"><select id="review-mode" class="select"><option value="single">Single</option><option value="double-blind" selected>Double blind</option><option value="adjudicated">Adjudicated</option></select><select id="review-assignment" class="select"><option value="round-robin" selected>Round robin</option><option value="deterministic-random">Deterministic random</option><option value="manual">Manual</option></select><input id="review-count" class="field" type="number" min="1" max="20" value="2"><input id="review-sla" class="field" type="number" min="1" value="24" placeholder="SLA hours"><input id="review-program-reviewers" class="field mono" style="grid-column:1/-1" placeholder="Reviewer member IDs, comma-separated"><select id="review-adjudication" class="select" style="grid-column:1/-1"><option value="">No adjudication queue</option>'+adjudication+'</select></div><button id="create-review-program" class="btn" style="margin-top:9px">Create program</button></div></section></div>'+(state.selectedQueue?'<section class="card" style="margin-top:14px"><div class="card-head">Assign a target to the selected program</div><div class="card-body"><div style="display:grid;grid-template-columns:180px 1fr;gap:8px"><select id="review-target-type" class="select"><option value="trace">Trace</option><option value="span">Span</option><option value="experiment-case">Experiment case</option></select><input id="review-target-id" class="field mono" placeholder="Target ID"><input id="review-target-subid" class="field mono" placeholder="Optional span/case ID"><input id="review-target-reviewers" class="field mono" placeholder="Override reviewer IDs, comma-separated"><input id="review-gold" class="field" style="grid-column:1/-1" placeholder="Optional gold label for calibration"></div><button id="assign-review-target" class="btn secondary" style="margin-top:9px">Create blinded assignments</button></div></section>':'')}
function bindReviewProgram(){const list=id=>el(id).value.split(',').map(x=>x.trim()).filter(Boolean);el('create-review-program').onclick=async()=>{const reviewers=list('review-program-reviewers'),mode=el('review-mode').value,count=Number(el('review-count').value),assignment=el('review-assignment').value;if(assignment!=='manual'&&reviewers.length<count){toast('Add at least '+count+' reviewer member IDs or choose manual assignment');return}const body={name:el('review-queue-name').value,mode,assignment,reviewersPerTarget:count,slaHours:Number(el('review-sla').value),reviewerIds:reviewers};if(mode==='adjudicated'&&el('review-adjudication').value)body.adjudicationQueueId=el('review-adjudication').value;await api(base()+'/queues',{method:'POST',body:JSON.stringify(body)});toast('Review program created');reviews()};if(el('assign-review-target'))el('assign-review-target').onclick=async()=>{const reviewers=list('review-target-reviewers'),target={type:el('review-target-type').value,id:el('review-target-id').value};if(el('review-target-subid').value)target.subId=el('review-target-subid').value;const body={target};if(reviewers.length)body.reviewerIds=reviewers;if(el('review-gold').value)body.goldLabel=el('review-gold').value;await api(base()+'/queues/'+encodeURIComponent(state.selectedQueue)+'/assign',{method:'POST',body:JSON.stringify(body)});toast('Assignments created');reviews()}}
function reviewPanel(item){if(!item)return '<div class="empty">Select an item to review its target.</div>';return '<h3 style="margin-top:0">Review decision</h3><div class="target"><div class="muted">Target</div><strong>'+esc(item.target.type)+' · '+esc(item.target.id)+'</strong>'+(item.target.subId?'<div class="mono muted">'+esc(item.target.subId)+'</div>':'')+'</div><div class="muted">Score <strong id="score-label">'+(item.score??.5)+'</strong></div><input id="review-score" class="score" type="range" min="0" max="1" step="0.05" value="'+attr(item.score??.5)+'"><input id="review-label" class="field" placeholder="Label (correct, hallucination, unsafe…)" value="'+attr(item.label||'')+'"><textarea id="review-comment" class="field" placeholder="Evidence and reviewer notes…">'+esc(item.comment||'')+'</textarea><div class="review-actions">'+(item.status==='pending'?'<button id="claim" class="btn secondary">Claim</button>':'')+'<button id="complete" class="btn">Save decision</button><button id="skip" class="btn danger">Skip</button>'+(item.target.type==='trace'?'<button id="promote-review" class="btn secondary">Promote to test</button>':'')+'</div><p class="muted">Revision '+num(item.revision)+' · '+esc(item.assignedTo||'unassigned')+'</p>'}
function bindReview(){const item=state.selectedReview;if(!item)return;el('review-score').oninput=()=>el('score-label').textContent=el('review-score').value;if(el('claim'))el('claim').onclick=async()=>{const body={assignee:me.principal.memberId||me.principal.keyId,revision:item.revision};const out=await api(base()+'/queues/'+encodeURIComponent(item.id)+'/claim',{method:'POST',body:JSON.stringify(body)});state.selectedReview=out.item;toast('Review claimed');reviews()};const complete=async skipped=>{const body={score:Number(el('review-score').value),label:el('review-label').value,comment:el('review-comment').value,revision:item.revision};if(skipped)body.status='skipped';const out=await api(base()+'/queues/'+encodeURIComponent(item.id)+'/complete',{method:'POST',body:JSON.stringify(body)});state.selectedReview=out.item;toast(skipped?'Review skipped':'Decision saved');reviews()};el('complete').onclick=()=>complete(false);el('skip').onclick=()=>complete(true);if(el('promote-review'))el('promote-review').onclick=()=>promoteTrace(item.target.id,{annotationItemId:item.id,onlineResultId:item.metadata?.onlineResultId})}
function eventTable(items){if(!items.length)return '<div class="empty">No analytics events in this range.</div>';return '<div class="table-scroll"><table class="table"><thead><tr><th>Resource</th><th>Status</th><th>Model / provider</th><th>Latency</th><th>Tokens</th><th>Time</th></tr></thead><tbody>'+items.map(x=>'<tr data-open="resource" data-kind="'+attr(x.kind)+'" data-id="'+attr(x.id)+'"><td><strong>'+esc(x.name)+'</strong><div class="mono muted truncate" style="max-width:230px">'+esc(x.id)+'</div></td><td>'+status(x.status)+'</td><td>'+esc(x.model||'—')+'<div class="muted">'+esc(x.provider||'')+'</div></td><td>'+ms(x.durationMs)+'</td><td>'+num(x.tokens)+'</td><td>'+date(x.occurredAt)+'</td></tr>').join('')+'</tbody></table></div>'}
function traceTable(items){if(!items.length)return '<div class="empty">No traces yet. Instrument an agent or ingest OpenTelemetry spans.</div>';return '<div class="table-scroll"><table class="table"><thead><tr><th>Trace</th><th>Status</th><th>Spans</th><th>Latency</th><th>Tags</th><th>Started</th></tr></thead><tbody>'+items.map(x=>'<tr data-open="local" data-kind="trace" data-id="'+attr(x.id)+'"><td><strong>'+esc(x.name)+'</strong><div class="mono muted">'+esc(x.id)+'</div></td><td>'+status(x.status)+'</td><td>'+num(x.spans.length)+'</td><td>'+ms(x.durationMs)+'</td><td>'+tags(x.tags)+'</td><td>'+date(x.startedAt)+'</td></tr>').join('')+'</tbody></table></div>'}
function experimentTable(items){if(!items.length)return '<div class="empty">No experiments yet. Run a dataset through Dry Run eval.</div>';return '<div class="table-scroll"><table class="table"><thead><tr><th>Experiment</th><th>Result</th><th>Dataset</th><th>Cases</th><th>Score metrics</th><th>Updated</th></tr></thead><tbody>'+items.map(x=>'<tr data-open="local" data-kind="experiment" data-id="'+attr(x.id)+'"><td><strong>'+esc(x.name)+'</strong><div class="mono muted">'+esc(x.id)+'</div></td><td>'+status(x.passed?'passed':'failed')+'</td><td>'+esc(x.dataset.name)+'</td><td>'+num(x.summary.total)+'</td><td>'+num(x.aggregates.length)+'</td><td>'+date(x.updatedAt)+'</td></tr>').join('')+'</tbody></table></div>'}
function bindOpenRows(){document.querySelectorAll('[data-open]').forEach(row=>row.onclick=()=>row.dataset.open==='resource'?openResource(row.dataset.kind,row.dataset.id):openLocal(row.dataset.kind,row.dataset.id))}
async function openLocal(kind,id){const endpoint=kind==='trace'?'/traces/':'/experiments/';const body=await api(base()+endpoint+encodeURIComponent(id));openDrawer(kind,body[kind==='trace'?'traces':'experiments'][0])}async function openResource(kind,id){try{const body=await api(base()+'/analytics/resources/'+kind+'/'+encodeURIComponent(id));openDrawer(kind,body.resource.payload)}catch{await openLocal(kind,id)}}
function openDrawer(kind,data){const title=data.name||data.id;el('drawer-root').innerHTML='<div class="drawer-backdrop" onclick="closeDrawer()"></div><aside class="drawer"><header class="drawer-head"><div><div class="drawer-title">'+esc(title)+'</div><div class="mono muted">'+esc(data.id)+'</div></div><span class="spacer"></span>'+status(kind==='trace'?data.status:(data.passed?'passed':'failed'))+(kind==='trace'?'<button id="promote-trace" class="btn small">Promote to test</button>':'')+'<button class="btn secondary small" onclick="closeDrawer()">Close</button></header><div class="drawer-body"><div class="tabs"><button class="active" data-tab="detail">'+(kind==='trace'?'Timeline':'Scores')+'</button><button data-tab="conversation">Conversation</button><button data-tab="raw">Raw JSON</button></div><div id="tab-body">'+detailView(kind,data)+'</div></div></aside>';if(el('promote-trace'))el('promote-trace').onclick=()=>promoteTrace(data.id);document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{document.querySelectorAll('[data-tab]').forEach(x=>x.classList.toggle('active',x===b));el('tab-body').innerHTML=b.dataset.tab==='raw'?'<pre class="raw">'+esc(JSON.stringify(data,null,2))+'</pre>':b.dataset.tab==='conversation'?conversationView(kind,data):detailView(kind,data)})}
async function promoteTrace(id,metadata={}){const name=prompt('Regression name (optional)')||undefined;const out=await api(base()+'/traces/'+encodeURIComponent(id)+'/promote',{method:'POST',body:JSON.stringify(Object.assign({},metadata,name?{name}:{}))});closeDrawer();toast(out.regression.manifest.cassette?'Dataset, cassette and test created':'Dataset created with warning');return out}
function detailView(kind,data){if(kind==='trace'){const depths={};const byId={};(data.spans||[]).forEach(s=>byId[s.id]=s);function depth(s){if(depths[s.id]!=null)return depths[s.id];let d=0,p=s.parentId,seen=new Set;while(p&&byId[p]&&!seen.has(p)){seen.add(p);d++;p=byId[p].parentId}return depths[s.id]=Math.min(d,12)}return '<div class="muted" style="margin-bottom:12px">'+num(data.spans?.length||0)+' spans · '+ms(data.durationMs)+'</div>'+(data.spans||[]).map(s=>'<div class="span" style="margin-left:'+depth(s)*18+'px"><div class="span-top"><strong>'+esc(s.name)+'</strong>'+status(s.status)+'</div><div class="duration">'+esc(s.type)+' · '+ms(s.durationMs||0)+'</div></div>').join('')}return '<div class="kpis">'+(data.aggregates||[]).slice(0,10).map(a=>'<div class="card kpi"><div class="muted">'+esc(a.name)+'</div><div class="value">'+pct(a.mean)+'</div><div class="delta">'+pct(a.passRate)+' pass rate · 95% CI '+pct(a.confidence95?.low||0)+'–'+pct(a.confidence95?.high||0)+'</div></div>').join('')+'</div><h3>Cases</h3>'+(data.results||[]).map(r=>'<div class="span"><div class="span-top"><strong>'+esc(r.name||r.caseId)+'</strong>'+status(r.passed?'passed':'failed')+'</div><div class="duration">'+ms(r.durationMs)+' · '+(r.scores||[]).map(s=>esc(s.name)+': '+pct(s.score)).join(' · ')+'</div></div>').join('')}
function conversationView(kind,data){const entries=[];if(kind==='trace'){(data.spans||[]).forEach(s=>{if(s.input!==undefined)entries.push({role:(s.type==='llm'?'user':s.type)+' input',value:s.input});if(s.output!==undefined)entries.push({role:(s.type==='llm'?'assistant':s.type)+' output',value:s.output})})}else{(data.results||[]).forEach(r=>{entries.push({role:'input',value:r.input});if(r.output!==undefined)entries.push({role:'output',value:r.output})})}return entries.length?entries.map(x=>'<div class="message"><div class="role">'+esc(x.role)+'</div><pre class="raw" style="margin:0">'+esc(typeof x.value==='string'?x.value:JSON.stringify(x.value,null,2))+'</pre></div>').join(''):'<div class="empty">No conversation payload captured.</div>'}
function head(title,copy,actions=''){return '<div class="view-head"><div><h2>'+esc(title)+'</h2><div class="muted">'+esc(copy)+'</div></div>'+actions+'</div>'}function kpis(values){return '<div class="kpis">'+values.map(x=>'<div class="card kpi"><div class="muted">'+esc(x[0])+'</div><div class="value">'+esc(x[1])+'</div><div class="delta muted">'+esc(x[2])+'</div></div>').join('')+'</div>'}function healthRow(label,value,percent){return '<div class="facet"><div class="facet-row"><span>'+esc(label)+'</span><strong>'+esc(value)+'</strong></div><div class="bar"><span style="width:'+Math.max(2,percent)+'%"></span></div></div>'}function facetBlocks(f){const groups=[['Status',f.status],['Models',f.model],['Providers',f.provider],['Releases',f.release],['Tags',f.tags]];return groups.map(g=>{const max=Math.max(1,...(g[1]||[]).map(x=>x.count));return '<div class="facet"><div class="muted" style="margin-bottom:8px">'+g[0]+'</div>'+((g[1]||[]).slice(0,4).map(x=>healthRow(x.value,num(x.count),x.count/max*100)).join('')||'<span class="muted">No values</span>')+'</div>'}).join('')}
function chart(points){if(!points.length)return '<div class="empty">No time-series data.</div>';const w=700,h=210,p=16;const xy=points.map((x,i)=>[(points.length===1?w/2:p+i*(w-2*p)/(points.length-1)),h-p-(x.passRate||0)*(h-2*p)]);const line=xy.map(x=>x.join(',')).join(' ');const area=p+','+(h-p)+' '+line+' '+(w-p)+','+(h-p);return '<svg viewBox="0 0 '+w+' '+h+'" preserveAspectRatio="none"><defs><linearGradient id="area" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#0a84ff" stop-opacity=".35"/><stop offset="1" stop-color="#0a84ff" stop-opacity="0"/></linearGradient></defs><line class="gridline" x1="0" y1="'+p+'" x2="'+w+'" y2="'+p+'"/><line class="gridline" x1="0" y1="'+h/2+'" x2="'+w+'" y2="'+h/2+'"/><line class="gridline" x1="0" y1="'+(h-p)+'" x2="'+w+'" y2="'+(h-p)+'"/><polygon class="area" points="'+area+'"/><polyline class="line" points="'+line+'"/></svg>'}
function mergeQuery(query,part){return query?(query+'&'+part):('?'+part)}function status(value){const x=String(value||'unknown').toLowerCase();return '<span class="status '+attr(x)+'">'+esc(x)+'</span>'}function tags(values){return (values||[]).slice(0,3).map(x=>'<span class="tag">'+esc(x)+'</span>').join('')||'—'}function num(x){return Number(x||0).toLocaleString()}function pct(x){return (Number(x||0)*100).toFixed(1)+'%'}function signedPct(x){x=Number(x||0)*100;return (x>0?'+':'')+x.toFixed(2)+' pp'}function avg(values){return values&&values.length?values.reduce((sum,x)=>sum+Number(x||0),0)/values.length:0}function ms(x){x=Number(x||0);return x>=1000?(x/1000).toFixed(2)+' s':x.toFixed(x<10?1:0)+' ms'}function usd(x){return '$'+Number(x||0).toFixed(4)}function bytes(x){x=Number(x||0);const u=['B','KB','MB','GB','TB'];let i=0;while(x>=1024&&i<u.length-1){x/=1024;i++}return (i&&x<10?x.toFixed(1):Math.round(x))+' '+u[i]}function date(x){try{return new Intl.DateTimeFormat(undefined,{month:'short',day:'numeric',hour:'2-digit',minute:'2-digit'}).format(new Date(x))}catch{return '—'}}function esc(x){return String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function attr(x){return esc(x).split(String.fromCharCode(96)).join('&#96;')}async function safeApi(path){try{return await api(path)}catch{return {}}}function closeDrawer(){el('drawer-root').innerHTML=''}function toast(message){el('toast').textContent=message;el('toast').classList.remove('hidden');setTimeout(()=>el('toast').classList.add('hidden'),2200)}function toggleTheme(){document.body.style.filter=document.body.style.filter?'':'contrast(1.06) brightness(1.04)'}
const fragment=new URLSearchParams(location.hash.slice(1)).get('token');fetch('/api/v1/auth/config').then(r=>r.json()).then(c=>{if(c.oidc)el('oidc').classList.remove('hidden')});if(fragment){history.replaceState(null,'',location.pathname);el('token').value=fragment;connect()}
</script></body></html>`;
